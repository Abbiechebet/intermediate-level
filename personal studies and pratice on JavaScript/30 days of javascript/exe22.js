/**
 * Manipulating DOM Object (Day 2)
 * 
Creating an Element
   To create an HTML element we use tag name.
   We use the method document.createElement()
To create multiple elements we should use loop. 

Appending child to a parent element
   To see a created element on the HTML document we 
   should append it to the parent as a child element.
We can access the HTML document body using document.body. 
   The document.body support the appendChild() method.

Removing a child element from a parent node
     After creating an HTML, we may want to remove element or 
     elements and we can use the removeChild() method
 */